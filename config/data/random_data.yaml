_target_: data.base_data_module.BaseDataModule

opt:
  data_dir:
  train:
    dataset:
      _target_: data.RandomSet.RandomDataSet
      num_samples: 1000
      transforms:


    dataloader:
      batch_size: 10
      num_workers: 4
      shuffle: True
      pin_memory: True

  val:
    dataset:
      _target_: data.RandomSet.RandomDataSet
      num_samples: 100
      transforms: 
  
    dataloader:
      batch_size: 1
      num_workers: 2
      # shuffle: True
      # pin_memory: True

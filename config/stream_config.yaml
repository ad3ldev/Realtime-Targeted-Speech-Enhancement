name: stream
hydra:
  job:
    name: ${name}
  run:
    dir: results/${name}/${now:%Y-%m-%d_%H-%M-%S}

logger:
  use_tb_logger: True

streaming:
  fs: 16000
  window_size: 30   # ms

defaults:
- model: PDenoiser
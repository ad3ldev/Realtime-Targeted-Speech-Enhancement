\section{Personalized Denoiser}

\subsection{Motivation}
The Denoiser model \cite{defossez2020} offers raw waveform speech enhancement using an encoder-decoder architecture with a U-Net structure.
Altough the model great performance, it lacks the ability to personalize the enhancement process.
To introduce personalization, we propose a personalized denoiser model or simply PDenoiser. PDernoiser consists of two major modules: a speaker embedding module and a denoising module.
The speaker embedding module is responsible for extracting speaker-specific features from the input speech signal, while the denoising module is responsible for enhancing the speech signal based on the extracted speaker features.
The architecture of the PDenoiser model is shown in Figure \ref{fig:pdenoiser-architecture}.

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.8\textwidth]{images/experiments/pdenoiser/pdenoiser-arch.png}
    \caption{PDenoiser Architecture}
    \label{fig:pdenoiser-architecture}
\end{figure}

\subsection{Speaker Embedding Module}
We primarily utilize TitaNet \cite{koluguri2021} to extract speaker embeddings from input speech signals of size 192.
These speaker embeddings are then processed through a block, repeated twice, comprising a linear layer, followed by layer normalization, a ReLU activation function, and a dropout layer with a rate of 0.4.
This block serves to enhance the dimensionality of the speaker embeddings to 768.

\subsection{Denoising Module}
The denoising module employs a standard Denoiser model. The output from the last encoder layer is scaled by a factor of $W_{enh}$  set to 0.999, and then combined with the output from the speaker embedding module, scaled by $W_{emb}$ which is set to 0.001.

\subsection{Experimental Setup}
We trained the PDenoiser model on the training set of our dataset for 12 epochs with a batch size of 32 per GPU. We trained the model using Kaggle's 2 NVIDIA Tesla T4 GPUs.
The noisy audio clips were generated on the fly by mixing speakerA and speakerB clean audio clips. The SNR of the noisy audio clips was randomly selected from values of 0, 3, 6, 10 and 20.
The clean audio clips were cropped to 3 seconds before mixing as an augmentation technique. The audio clips were sampled at 16kHz.
The Adam optimizer was used with a learning rate of $1 \times 10^{-4}$ with Cosine Annealing \cite{loshchilov2017sgdr} and beta values of 0.9 and 0.999. Model exponential moving average (EMA) was used with a decay of 0.999.
We employed a mixture of loss functions, including L1 loss, Multi Resolution STFT loss and TAP loss \cite{zeng2023tap} with weights 1, 0.4 and 0.3 respectively.
Both variations of the speaker embedding module were frozen during the training of the PDenoiser model. For the speaker denoising module, we utilized the pretrained weights from Meta's Denoiser model.
Figure \ref{fig:pdenoiser-loss} illustrates the training loss curves of the PDenoiser model for the two variations.

\begin{figure}[H]
    \centering
    \begin{subfigure}{0.49\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/experiments/pdenoiser/train_l1_loss.png}
        \caption{L1 Loss}
    \end{subfigure}
    \begin{subfigure}{0.49\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/experiments/pdenoiser/train_mrstft_loss.png}
        \caption{Multi-Resolution STFT Loss}
    \end{subfigure}
    \begin{subfigure}{0.49\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/experiments/pdenoiser/train_tap_loss.png}
        \caption{TAP Loss}
    \end{subfigure}
    \caption{PDenoiser model training loss curves}
    \label{fig:pdenoiser-loss}
\end{figure}


\subsection{Results}
We tested the model on two test sets: personalized DNS blind test set and the test set of our dataset. Results are show in table \ref{tab:pdenoiser}.

\begin{table}[]
    \centering
    \begin{tabular}{ccccccc}
    \hline
    \multirow{2}{*}{Metric} & \multicolumn{5}{c}{Collected DataSet (Different SNR)} & \multirow{2}{*}{\begin{tabular}[c]{@{}c@{}}Personalized DNS\\ Blind set\end{tabular}} \\ %\cline{2-6}
                        & 0     & 3     & 6     & 10    & 20    &       \\ \hline
    DNSMOS/SIG          & 4.108 & 3.869 & 3.716 & 3.887 & 4.025 & 3.748 \\ 
    DNSMOS/BAK          & 1.725 & 2.248 & 3.475 & 3.874 & 3.988 & 2.824 \\ 
    DNSMOS/OVRL         & 2.183 & 2.458 & 3.086 & 3.413 & 3.575 & 2.711 \\ 
    PESQ nb             & 1.429 & 1.891 & 2.431 & 2.830 & 3.345 & -     \\ 
    PESQ wb             & 1.171 & 1.495 & 1.998 & 2.423 & 3.038 & -     \\ 
    STOI                & 0.690 & 0.849 & 0.913 & 0.941 & 0.962 & -     \\ \hline
    \end{tabular}
    \caption{PDenoiser model results on the collected dataset test set using different SNR levels and on the personalized DNS blind test set}
    \label{tab:pdenoiser}
\end{table}

\subsection{Limitations}
Upon listening to the outputs of the PDenoiser model on our dataset we noticed that it struggles when the background speaker(s) noise is loud it fails to fully suppress it. The model size is large compared to Personalized FFSN model.
